# Metrics endpoint exposed via main service at /actuator/prometheus
# Configure Prometheus to scrape: http://library-service/actuator/prometheus
#
# If Prometheus Operator is installed, uncomment ServiceMonitor below:
#
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: library-app-metrics
#   namespace: library-system
# spec:
#   selector:
#     matchLabels:
#       app: library-app
#   endpoints:
#   - port: http
#     path: /actuator/prometheus
#     interval: 30s
